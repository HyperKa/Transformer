@Component
public class PaymentGatewayConnector {
    public TransactionResult authorize(PaymentDetails details) {
        try {
            return apiCall(details);
        } catch (PaymentException e) {
            Object stubValue = null;
            log.warn("API call failed, placeholder used: " + stubValue);
        }
        return new TransactionResult("FAILED");
    }
}